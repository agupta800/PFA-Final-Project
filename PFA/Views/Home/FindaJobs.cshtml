
@{
    ViewData["Title"] = "FindaJobs";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<main>

    <!-- Hero Area Start-->
    <div class="slider-area">
        <div class="single-slider section-overly slider-height2 d-flex align-items-center"
             style="background-image: url('@Url.Content("~/assets/img/hero/about1.jpg")')">
            <div class="container">
                <div class="row">
                    <div class="col-xl-12">
                        <div class="hero-cap text-center">
                            <h2>Get your job</h2>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Hero Area End -->
    <!-- Job List Area Start -->
    <div class="container mt-5">
        <div class="row justify-content-center">
@*             <div class="col-md-8">
                @foreach (AdminModel item in Model)
                {
                    <div class="card mb-4">
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-3">
                                    <img src="@item.companyImageUrl" class="img-fluid" alt="@item.companyname" />
                                </div>
                                <div class="col-md-9">
                                    <h4 class="card-title"><a href='@item.applylink' target="_blank">@item.jobTitle</a></h4>
                                    <ul class="list-unstyled">
                                        <li>@item.companyname</li>
                                        <li>@item.jobTitle</li>
                                        <li>@item.inputDate</li>
                                    </ul>
                                </div>
                            </div>
                        </div>
                        <div class="card-footer text-muted d-flex justify-content-between">
                            <a href='@item.applylink' target="_blank" class="btn btn-primary">Apply Link</a>
                            <span class="text-muted">7 hours ago</span>
                        </div>
                    </div>
                }
            </div>
 *@        </div>
    </div>
</main>


